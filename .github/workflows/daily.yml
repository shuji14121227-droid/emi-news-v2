name: Daily Research Agent
on:
  schedule:
    - cron: '0 0 * * *'  # 毎日 台湾時間 08:00 に実行
  workflow_dispatch:     # 手動実行ボタン
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install tools
        run: pip install -r requirements.txt
      - name: Run Agent
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py
      - name: Update Report
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add README.md history.txt index.html styles.css archive
          git commit -m "Update Daily Report and History" || exit 0
          git push
